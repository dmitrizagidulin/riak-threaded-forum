<h2>New Discussion</h2>
<dl class="dl-horizontal">
  <dt>Forum:</dt>
  <dd><%= @forum.name %></dd>
  <dt>Author:</dt>
  <dd><%= @current_user.username %></dd>
</dl>

<%= render partial: 'forum_posts/form', locals: { create_path: forum_discussions_path(@forum) } %>

<%= link_to 'Back', forum_posts_path %>
